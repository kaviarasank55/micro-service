spring:
  application:
    name: service-1  # This name will be used in Consul

  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}  # Uses the same name
        enabled: true
        register: true
